name: code-sentinel-ai CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'code-sentinel-ai/**'
      - '.github/workflows/code-sentinel-ai-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'code-sentinel-ai/**'

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  test-and-build:
    name: Test & Build
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [20.x, 22.x]
    
    steps:
    - uses: actions/checkout@v5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: code-sentinel-ai/package-lock.json
    
    - name: Install dependencies
      working-directory: ./code-sentinel-ai
      run: npm ci
    
    - name: Run linter
      working-directory: ./code-sentinel-ai
      run: npm run lint
    
    - name: Run type check
      working-directory: ./code-sentinel-ai
      run: npm run typecheck
    
    - name: Run tests with coverage
      working-directory: ./code-sentinel-ai
      run: npm run test:coverage
    
    - name: Generate test summary
      if: matrix.node-version == '20.x'
      working-directory: ./code-sentinel-ai
      run: |
        echo "## 🧪 Test Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ -f "coverage/coverage-summary.json" ]; then
          echo "### Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          cat coverage/coverage-summary.json | jq '.total' >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        fi
        echo "✅ Tests passed on Node.js ${{ matrix.node-version }}" >> $GITHUB_STEP_SUMMARY
    
    - name: Upload coverage reports
      if: matrix.node-version == '20.x'
      uses: actions/upload-artifact@v6
      with:
        name: coverage-reports
        path: |
          code-sentinel-ai/coverage/lcov.info
          code-sentinel-ai/coverage/coverage-final.json
          code-sentinel-ai/coverage/lcov-report/
        retention-days: 30
    
    - name: Upload test results
      if: always() && matrix.node-version == '20.x'
      uses: actions/upload-artifact@v6
      with:
        name: test-results
        path: code-sentinel-ai/coverage/
        retention-days: 7
    
    - name: Build production bundle
      working-directory: ./code-sentinel-ai
      run: npm run build
    
    - name: Upload coverage to Codecov
      if: matrix.node-version == '20.x'
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ./code-sentinel-ai/coverage/lcov.info
        flags: code-sentinel-ai
        name: code-sentinel-ai-coverage
        fail_ci_if_error: false
    
    - name: Commit and push code-sentinel-ai build artifacts
      if: matrix.node-version == '20.x' && github.event_name == 'push' && github.ref_name != 'main' && github.actor != 'github-actions[bot]'
      run: |
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git config user.name "github-actions[bot]"
        git add -f code-sentinel-ai/dist code-sentinel-ai/coverage
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "chore: update code-sentinel-ai build artifacts [skip ci]"
          git push origin HEAD:${{ github.ref_name }}
        fi

  validate:
    name: Validate Action
    runs-on: ubuntu-latest
    needs: test-and-build
    
    steps:
    - uses: actions/checkout@v5
    
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: code-sentinel-ai/package-lock.json
    
    - name: Install dependencies
      working-directory: ./code-sentinel-ai
      run: npm ci
    
    - name: Validate action.yml
      working-directory: ./code-sentinel-ai
      run: |
        if [ ! -f "action.yml" ]; then
          echo "❌ action.yml not found"
          exit 1
        fi
        echo "✅ action.yml exists"
    
    - name: Build and check dist folder
      working-directory: ./code-sentinel-ai
      run: |
        npm run build
        if [ ! -f "dist/index.js" ]; then
          echo "❌ dist/index.js not found after build"
          exit 1
        fi
        echo "✅ Build artifacts present"
        
        # Check bundle size
        SIZE=$(du -h dist/index.js | cut -f1)
        echo "📦 Bundle size: $SIZE"
